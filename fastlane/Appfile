# 더잔잔 (The Janjan)
#
# 값은 전부 GitHub Secrets 에서 온다. 저장소에 Apple ID·팀 ID 를 적지 않는다.

app_identifier("com.thejanjan.app")
team_id(ENV["APPLE_TEAM_ID"])

# Apple ID 로그인은 쓰지 않는다. App Store Connect API 키 방식만 사용한다.
# (2단계 인증 세션이 만료되어 CI 가 멈추는 사고를 피하려고)
